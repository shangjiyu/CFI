import SwiftUI

struct PanelView: View {
    var body: some View {
        NavigationView {
            Form {
                
            }
            .navigationBarTitle("面板")
            .navigationBarTitleDisplayMode(.inline)
        }
    }
}
